import { FlashAnimationDirective } from './flash-animation.directive';

describe('FlashAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new FlashAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
